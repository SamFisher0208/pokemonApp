# Pokemon App with React and GraphQL

Created by following along with Scrimba's 'Build an App with React and GraphQL' course.

It reads Pokemon data from a GraphQL server and displays it in a simple flex format.
