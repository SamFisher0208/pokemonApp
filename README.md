# Pokemon App with React and GraphQL

Created following along with Scrimba's 'Build an App with React and GraphQL' course.

It reads Pokemon data from a GraphQL server and displays in a simple flex format.
